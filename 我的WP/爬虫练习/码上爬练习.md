# 请求头检测
要求爬取100页的数字并计算总和

```python
import httpx

headers = {
  "accept": "application/json, text/javascript, */*; q=0.01",
  "accept-encoding": "gzip, deflate, br, zstd",
  "accept-language": "zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6",
  "cookie": "sessionid=bm6ssythck6aaw8qscxbwx324qhg3fyy",
  "priority": "u=1, i",
  "referer": "https://www.spiderdemo.cn/sec1/header_check/",
  "sec-ch-ua": "\"Microsoft Edge\";v=\"141\", \"Not?A_Brand\";v=\"8\", \"Chromium\";v=\"141\"",
  "sec-ch-ua-mobile": "?0",
  "sec-ch-ua-platform": "\"Windows\"",
  "sec-fetch-dest": "empty",
  "sec-fetch-mode": "cors",
  "sec-fetch-site": "same-origin",
  "user-agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/141.0.0.0 Safari/537.36 Edg/141.0.0.0",
  "x-requested-with": "XMLHttpRequest"
}

total_sum = 0
for i in range(1, 101):
    url = f"https://www.spiderdemo.cn/sec1/api/challenge/page/{i}/?challenge_type=header_check"
    response = httpx.get(url, headers=headers)
    total_sum += sum(response.json()["page_data"])
    print(f"Current sum is: {total_sum}")
print(f"total sum is: {total_sum}")
```
## 一些学到的知识
- fetch/XHR(XmlHttpRequest): 网页在后台向服务器取得请求，拿到需要更新的数据，然后更新页面。可以理解为部分更新的操作，只更新需要更新的部分。
- cookie: 浏览器给服务器发的“通行证 + 身份证 + 临时凭证”

通过cookie的设置，可以伪装成正常用户发送的请求，从而让服务器放行请求
## 二编
发现我的代码比别人的慢多了，问了一下ai，主要原因出先在这个地方。

```python
total_sum = 0
for i in range(1, 101):
    url = f"https://www.spiderdemo.cn/sec1/api/challenge/page/{i}/?challenge_type=header_check"
    response = httpx.get(url, headers=headers)
    total_sum += sum(response.json()["page_data"])
    print(f"Current sum is: {total_sum}")
print(f"total sum is: {total_sum}")
```

我每次都重新建立一个http请求，相当于每次都要经历tcp/ip三次握手，所以我们最好直接复用连接。
同时我还添上了很多浏览器的coockie（似乎也会导致速度变慢？）下面给出改进过的代码。

```python
import httpx

headers = {
  "user-agent": "Mozilla/5.0 ...",
  "referer": "https://www.spiderdemo.cn/sec1/header_check/",
  "x-requested-with": "XMLHttpRequest"
}

cookies = {
    'Hm_lvt_0d2227abf9548feda3b9cb6fddee26c0': '1767170985',
    'sessionid': 'jgck2qlka5mhve0wciiikj8y08a9a38e'
}

total_sum = 0

with httpx.Client(headers=headers, cookies=cookies, timeout=10) as client:
    for i in range(1, 21):
        url = f"https://www.mashangpa.com/api/problem-detail/2/data/?page={i}"
        resp = client.get(url)
        arr = resp.json().get("current_array", [])
        print(i, len(arr))
        total_sum += sum(arr)

print("total:", total_sum)

```